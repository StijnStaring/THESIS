CasADI integration --> because of constant --> explicit euler --> data is available --> more suitable to do crank-nicolson integration
______________________________________________________________________________________________________________________________________

ode = {'x':sol1,'p':vertcat(axs,ays),'ode':f1}

    options = dict()
    options["tf"] = dt_grid
    intg = integrator('intg', 'rk', ode, options)
    print(intg)
    res = intg(x0 = 1, p=[1,1])
    print("xf = ", res["xf"])